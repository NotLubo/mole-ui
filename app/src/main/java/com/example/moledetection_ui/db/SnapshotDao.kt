package com.example.moledetection_ui.db

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query
/*
@Dao
interface SnapshotDao {
    @Query("SELECT * FROM snapshot WHERE parent_lesion_id=:lesionId")
    fun getSnapshots(lesionId: Int) : List<Snapshot>

    @Insert
    suspend fun insert(snapshot: Snapshot)
}*/